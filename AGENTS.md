# 專案方向

## 核心目標

讓 AI Agent 以少量查詢，快速找到與任務相關的程式碼檔案，並取得完整、可核對的來源與關係證據。

功能與架構決策應服務於檔案查找品質、證據完整性與查詢效率，建立 SymbolLattice 自己的特色。每項改動都應有實際使用價值，避免只為增加功能數而增加複雜度。

## 結果要求

- 依查詢意圖（例如解釋流程、定位問題或準備修改）判定檔案相關性，優先找出完成任務所必需的檔案，並區分必要檔案與補充脈絡，減少遺漏與無關內容。
- 說明每個檔案為什麼相關，附上來源路徑、行號與必要原始碼。
- 每個相關性與關係判斷應能對應到實際來源，路徑、行號、原始碼與關係描述須互相一致。跨檔路徑中的每一步都應有可核對的連結依據，無法連接的部分必須揭露。
- 區分已確認、推測與未解析的關係，明示來源或索引的新鮮度與無法確認之處。
- 區分沒有找到結果、解析能力不足、索引過期、搜尋範圍受限與輸出被截斷；沒有找到關係，不代表關係不存在，也不能據此保證修改或刪除不會造成影響。
- 輸出受限時，標示省略範圍並提供後續查詢方式；不將截斷或局部結果描述為完整結果。

證據完整是指判斷有依據、關係可追溯、缺口有揭露，不要求每次回傳所有檔案或整份原始碼。

## 目前優先方向

1. 相關檔案的召回與排序品質。
2. 來源與跨檔關係的證據完整性。
3. 查詢速度、索引更新效率與輸出成本。
4. Agent 的查詢便利性與後續追查能力。

優先避免遺漏任務必要檔案，透過排序、分層與後續查詢控制噪音和輸出量。不為增加召回而把推測關係當成確定事實，也不為縮短回應而隱藏關鍵證據。

以重要檔案遺漏、無關結果、證據可核對程度、查詢延遲與 Agent 補查次數評估改善。功能數、語言數或測試數不能單獨證明品質提升。

## 語言深度原則

- 語言支援應分別說明宣告擷取、同檔關係、跨檔解析、型別與框架語意的覆蓋範圍；文件、模板與設定格式依適用的結構與引用關係評估。
- 可掃描、有解析器或通過小型測試，不能單獨證明支援深度足夠；`project` 分類也不代表完整跨檔解析。
- 分開驗證關係解析是否正確，以及 Agent 是否找到任務必要檔案與可核對的證據；兩者不能互相替代。
- 深化某語言時，以真實專案中造成必要檔案遺漏或證據中斷的問題為優先，保留失敗案例及未用於調整規則的驗證樣本。
- 無法解析的語法、動態行為與外部依賴須揭露，不以猜測補成確定關係。
- 優先改善既有語言的實用深度；新增語言需有明確使用需求與驗證範圍。

各語言目前的能力、限制與驗證結果維護於 `src/domain/language-depth.ts` 及 `benchmarks/` 的設定與文件。語言深化的階段優先順序另行維護，依使用需求與實測結果調整。

## 大型專案驗證

- 延續使用 GitHub 上的真實大型專案驗證功能，不只依賴小型 fixtures 或單元測試。優先使用及維護 `benchmarks/` 中既有的驗證工具。
- 固定並記錄 repository URL、commit、產品版本、執行命令與測試條件，讓結果可以重現及比較。
- 真值應來自獨立工具、編譯器或人工核對，不使用產品自己的輸出直接充當正確答案。
- 分別評估關係解析品質與任務相關檔案的查找品質；關係正確不代表最後回傳的檔案完整。
- 查找測試應分別涵蓋已知符號、檔名或路徑的精確查找，只有自然語言描述且不知道符號名稱的探索，以及需要跨檔案證據的流程追蹤。探索題不可為提高命中率而額外加入答案中的符號或路徑線索。
- 每個查找任務應在評分前獨立定義必要檔案、必要關係或來源證據、可接受的替代證據與尚未確認的部分。不配合產品輸出修改真值；有爭議的案例另行核對，若真值確有錯誤，記錄修訂依據並用修訂後的真值重新評估比較版本。
- 分別量測首次索引、增量同步、查詢延遲，以及 Agent 取得足夠證據所需的總時間與查詢次數；在相同條件下與既有基準比較，不以單次查詢速度代替整體任務效率。
- 具體時間門檻、測試專案、任務與通過標準記錄於 benchmark 設定或驗證文件，依實測結果調整；本文件保留驗收原則。
- 在明確的評估範圍內統計 TP（正確命中）、FP（錯誤命中）、FN（遺漏），並計算 precision 與 recall。報告應說明評估單位、真值範圍、抽樣方法與分母；未測量或分母為零的指標明示為未測量或不適用。
- 真值未涵蓋的結果應標為待核對，不直接當成 FP；負向案例通過不代表整體沒有 FP，也不能取代整體 precision 評估。
- 保留失敗案例作為回歸測試，同時保留未用於調整規則的驗證專案或樣本，避免只針對已知專案改善。
- 修改解析、關係推導、搜尋或排序時，執行對應的真實專案驗證，檢查品質與效能是否退步。純文件修改不要求重跑大型語料。
- 明確區分驗證工具的契約測試與實際大型語料驗證；工具測試通過不代表已執行完整外部專案分析。缺少語料或執行環境時，揭露尚未驗證的範圍。
- 外部專案、索引與大型產物放在獨立驗證工作區，不混入產品原始碼；遵循 `benchmarks/README.md` 的產物存放規則。

品質宣稱必須有依據。明確區分已讀取的實作、文件宣稱與實測結果，不宣稱未經證實的優勢或獨有性。

## README 維護

- 每完成一版修改，都要同步更新 `README.md` 與 `README.en.md` 的版本，以及本次變更影響的功能、指令、安裝方式或限制；中英文內容須一致。
- README 以初次使用者需要的重點為主：專案用途、主要功能、安裝、快速開始與必要限制。文字簡短易懂，範例精簡且可執行。
- 實作細節、完整驗證數據、長篇語言矩陣與歷史變更放在對應文件，README 提供連結。簡化內容時仍須保留影響使用者操作的限制與破壞性變更提醒。
- 純文件或內部調整若未改變使用方式，更新版本並核對既有說明即可，不為每版新增重複段落或累積開發日誌。

## 方向調整

以上優先順序可依使用者需求與實測結果調整。方向變更時應更新本文件，取代過時指示，避免累積矛盾規則。

具體功能清單與階段計畫另行維護，避免本文件過度膨脹。依當前任務範圍落實，不將每個任務擴大成完整研究或效能測試，並遵循使用者的具體要求。

## 版本管理

- 每次完成一批專案修改都必須更新版本，包含程式碼、文件、設定與本檔案的修改；同一批修改更新一次，不需要每編輯一個檔案就遞增。
- 版本採 `major.minor.patch`，參照 [Semantic Versioning](https://semver.org/lang/zh-TW/)，依下列專案規則選擇幅度。使用者指定版本時，以指定版本為準，仍須揭露相容性影響。
- 同步更新 `package.json`、`package-lock.json` 的頂層與根套件版本、`src/version.ts`，以及中英文 README 的目前版本，確保一致。
- 除使用者另有指示，每完成一版修正或一批專案修改，更新版本並通過該批修改所需的檢查後，主動將本次變更 commit 並 push，不需再次等待使用者要求。
- 推送至目前分支既有的追蹤遠端；本專案既有遠端為 `origin`（`https://github.com/HsinPu/SymbolLattice.git`）。
- 驗證版本一致性，並在完成回覆中說明新版本、commit 與 push 結果；推送失敗或受阻時如實回報。此授權不包含建立 tag 或發布套件。

### 升版判定

以對外契約與使用者升級成本判定，不依修改檔案數、程式碼行數或累積修正次數判定。對外契約包含已文件化的 CLI／MCP 工具與參數、結構化輸出及證據欄位語意、公開程式 API、設定、支援的執行環境，以及索引相容與升級方式。

| 層級 | 適用情況 | 範例 |
| --- | --- | --- |
| patch（修訂版） | 維持對外契約的問題修正、效能改善、內部重構，以及文件、測試或開發設定調整。 | 修正已宣告能力的漏判／誤判、行號錯誤、索引同步問題，或在相同查詢契約下改善排序與輸出成本；`0.520.10 → 0.520.11`。 |
| minor（次版） | 向下相容的新能力，或將既有對外能力標記為棄用但仍保留支援。 | 新增 CLI／MCP 工具、可選參數、新語言／框架，或首次支援原先未宣告的語法與關係解析；`0.520.11 → 0.521.0`。 |
| major（主版，適用於 `1.0.0` 之後） | 破壞既有對外契約，使使用者必須調整呼叫、設定、環境或原本承諾的索引升級流程。 | 移除／重新命名工具或欄位、新增必填參數、改變欄位型別或證據語意、停止支援既有 Node.js 版本；`1.4.2 → 2.0.0`。 |

- 同一批包含多種變更時，依影響最高的層級升版一次；升 minor 時 patch 歸零，升 major 時 minor 與 patch 歸零。
- 修正錯誤結果造成命中檔案、排序或關係數量改變，本身不代表破壞相容性；須檢查是否改變已承諾的介面與語意，並依既有驗證原則檢查品質。
- 目前 `0.x` 階段，相容修正升 patch、新增能力升 minor；不相容變更也升 minor，但必須在 README 或相關升級文件中明確標示「破壞性變更」，說明受影響介面與遷移、重建索引或環境調整方式。這是本專案的開發期約定，不代表 minor 一律相容。
- `1.0.0` 用於宣告核心對外契約及索引升級策略穩定，並具備可重現的核心任務驗證與已知限制文件；不因 minor 數字大、功能數多或修改量大而自動升為 `1.0.0`，也不要求所有語言達到相同深度。
- 產品版本與內部索引、extractor／resolver 版本分別管理；相關格式或解析行為變動仍須處理索引失效、遷移或重建。既有契約允許且可自動完成的索引更新不單獨構成破壞性變更。
- commit 訊息或完成回覆須簡述本次選擇 patch、minor 或 major 的原因。新規則適用於後續修改，不回溯改寫已提交的歷史版本。
